# leak_check.py allowlist
#
# Format: one entry per line, "relative/path:line_number", optionally
# followed by a "# justification" comment. Blank lines and lines
# starting with '#' are ignored by the checker.
#
# Every entry here is a KNOWN, ACCEPTED exception — not a place to dump
# false positives without review. If leak_check.py flags something new,
# default to fixing the source file. Only add an entry here when there's
# a real justification, and flag it to the admin for a ruling if the
# justification is "pending a decision" rather than "definitely fine."
#
# See MAINTAINERS.md > "Known pending items" for the current status of
# items sealed in this session (below).

# [FORGE VALUES UNSEALED 2026-07-31 — Forge ruling C — supersedes the 2026-07-24 seal]
#
# Forge Roulette odds and payout: NO LONGER SEALED. Ruling C redesigned the
# mechanic so it needs no hidden value to operate — the 1-in-3 odds and +15
# payout now live openly in forge/SKILL.md by design. The three leak_check
# patterns that guarded this value class were retired (not allowlisted) in
# the same change, because the values are no longer secret. There is nothing
# to allowlist here for the Forge any more. The 2026-07-24 seal (which moved
# these values behind an admin reference that a distributed plugin could not
# actually ship) is retired: a value a player's own agent must read to run a
# mechanic cannot be secret from that player in an open-source plugin.
#
# [SEALED 2026-07-24 via the seal PR — Class Engine only]
#
# Class Engine name: removed from intake/SKILL.md step 2. Reworded from
# "Class Engine's daily menu" to "System's adaptive quest recommendations"
# to avoid naming sealed internals while keeping meaningful context
# (calibration questions help align quests to actual player needs).
#
# If either sealed value appears again in player-facing surfaces, it is
# an unintended leak — grep should find it with leak_check.py unchanged.
